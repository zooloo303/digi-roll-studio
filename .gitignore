# Build output
/target
**/*.rs.bk
# Assembled .app, .dmg and installer — release artefacts, rebuilt by
# packaging/ and attached to the GitHub release, never committed.
/dist/

# packaging/linux — makepkg's working directories and its output; the .desktop
# file and PKGBUILD are the committed build inputs.
/packaging/linux/src/
/packaging/linux/pkg/
/packaging/linux/*.pkg.tar.zst
/packaging/linux/*.tar.gz

# OS
.DS_Store

# Agent/editor scratch
.claude/

# Kept local, deliberately out of the public repo
#   local/            frozen phase log, pre-public commit log, hardware scratch
#   design_handoff_*  the two UI design-handoff packages some ui:: comments cite
/local/
/design_handoff_digi_roll_ui/
/design_handoff_digi_roll_ui_v2/
